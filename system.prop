#
# system.prop for Nothing Phone (1) / Spacewar
#

# -----------------------------------------------------------------------------
# ADB & Debugging (Recovery için Güvenliği Kapat)
# -----------------------------------------------------------------------------
ro.adb.secure=0
ro.debuggable=1
# Bu çok önemli: Recovery açılır açılmaz ADB aktif olsun
persist.sys.usb.config=adb
sys.usb.config=adb

# -----------------------------------------------------------------------------
# USB Controller (MTP ve ADB Sorunlarını Çözen Kod)
# -----------------------------------------------------------------------------
# Cihazının USB portunun gerçek adresi budur. 
# Bunu yazmazsan PC cihazı görmeyebilir.
sys.usb.controller=a600000.dwc3
persist.sys.isUsbOtgEnabled=true

# -----------------------------------------------------------------------------
# Dynamic Partitions
# -----------------------------------------------------------------------------
ro.boot.dynamic_partitions=true

# -----------------------------------------------------------------------------
# Crypto / Gatekeeper / FBE (Şifre Çözme Ayarları)
# -----------------------------------------------------------------------------
# Qualcomm Gatekeeper fix (Lag ve decryption hatalarını önler)
vendor.gatekeeper.disable_spu=true

# Android 12+ FBE Şifreleme standartları
ro.crypto.volume.filenames_mode=aes-256-cts
ro.crypto.allow_encrypt_override=true

# -----------------------------------------------------------------------------
# Battery & Charger
# -----------------------------------------------------------------------------
# Recovery modunda şarj olurken ekranı kapatabilsin
ro.charger.enable_suspend=1
